import { IfForDirective } from './if-for.directive';

describe('IfForDirective', () => {
  it('should create an instance', () => {
    const directive = new IfForDirective();
    expect(directive).toBeTruthy();
  });
});
